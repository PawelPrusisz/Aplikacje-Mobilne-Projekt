<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projekt_aplikacje">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projekt_Aplikacje"
        android:screenOrientation="portrait">
        <activity android:name=".ui.browse.NewRecipeActivity" />
        <activity android:name=".ui.browse.RecipeInfoActivity" />
        <activity android:name=".ui.diet_plan.DayDiet" />
        <activity android:name=".ui.diet_plan.DayDetails" />
        <activity android:name=".ui.diet_plan.WeeklyDietViewer" />
        <activity android:name=".ui.browse.BrowseActivity" />
        <activity
            android:name=".ui.main.MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>